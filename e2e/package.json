{
  "name": "flow-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "End-to-end tests for the Boomerang Flow product (client-web UI driving a real service-core backend, via the docker-compose stack at the repo root).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
